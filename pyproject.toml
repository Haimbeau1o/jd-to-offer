[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "jd-offer"
version = "0.1.0"
description = "Turn job descriptions into knowledge systems and project blueprints"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
  "typer>=0.12",
  "pydantic>=2.0",
  "PyYAML>=6.0",
  "jinja2>=3.1",
  "fastapi>=0.110",
  "uvicorn>=0.29",
  "httpx>=0.27",
]

[project.scripts]
jd-offer = "jd_offer.cli:main"
driverops-agent-lab = "driverops_agent_lab.cli:main"
commentops-agent-lab = "commentops_agent_lab.cli:main"

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
